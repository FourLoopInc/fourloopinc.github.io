# FourLoop static site

Open `index.html` in a browser to view the site. No installation, terminal command, or build step is required.

- `assets/css/style.css` — all styling and responsive Glass OS theme
- `assets/js/main.js` — theme toggle, mobile menu, filters, and form feedback
- `assets/images/` — put future image files here
